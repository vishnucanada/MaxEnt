# Core contribution (maxent/, experiments/, tests/)
numpy
scipy
pandas
matplotlib

# Tests (optional; tests also run via `python -m tests.run` without pytest)
pytest

# The legacy/ experiments need extra packages: see legacy/requirements.txt
