"""Legacy: the original maximum-entropy exploration (MLE / fidelity fitting).

Preserved for reference. This is the project's original method described in the
top-level README's history; it is NOT used by the current federated maxent-HDC
contribution (see the `maxent/` package). Self-contained except for path
constants imported from `maxent.data`.
"""
